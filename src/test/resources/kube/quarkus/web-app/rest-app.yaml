apiVersion: v1
kind: ServiceAccount
metadata:
  name: rest-app-service-account
  namespace: quarkus
---
apiVersion: v1
kind: Service
metadata:
  name: rest-app-service
  namespace: quarkus
  labels:
    app: rest-app
spec:
  ports:
  - name: http
    port: 8000
    targetPort: 8080
  selector:
    app: rest-app
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: rest-app-deployment
  namespace: quarkus
spec:
  replicas: 1
  selector:
    matchLabels:
      app: rest-app
      version: v1
  template:
    metadata:
      labels:
        app: rest-app
        version: v1
    spec:
      serviceAccountName: rest-app-service-account
      containers:
      - image: docker.pkg.github.com/explorviz/istio-playground/jvm:latest
        imagePullPolicy: IfNotPresent
        name: istio-playground
        ports:
        - containerPort: 8080